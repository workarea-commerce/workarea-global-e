source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'workarea', source: 'https://gems.weblinc.com'
gem 'workarea-gift_cards', source: 'https://gems.weblinc.com'

gemspec

group :test do
  gem 'simplecov', require: false
end
